<div class="mb-3 d-flex justify-content-between align-items-center">
  <%= link_to "← Back to Contestant", season_contestant_path(@season, @contestant), class: "text-decoration-none" %>
  <span class="badge bg-secondary"><%= @contestant.name %></span>
</div>

<div class="card shadow-sm">
  <div class="card-header bg-white">
    <h1 class="h4 mb-0">Edit Contestant</h1>
  </div>
  <div class="card-body">
    <%= render "form" %>
  </div>
</div>
